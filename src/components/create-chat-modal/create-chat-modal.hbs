<div class='{{styles.overlay}} {{#if isOpen}} {{styles.open}}{{/if}}'>
  <div class='{{styles.dialog}}'>
    <form class='{{styles.form}}'>
      <h2 class='{{styles.dialogTitle}}'>
        Create a New Chat
      </h2>
      {{{ChatTitleInput}}}

      <menu class='{{styles.dialogControls}}'>
        {{{CancelButton}}}
        {{{SubmitButton}}}
      </menu>
    </form>
  </div>
</div>
