<section class='{{styles.leftPanel}}'>
  <header class='{{styles.header}}'>
    <div class={{styles.logo}}>
      {{{logoIcon}}}
    </div>
    {{{SettingsButton}}}
  </header>

  <form class='{{styles.searchChat}}'>
    {{{SearchInput}}}
  </form>

  <ul class='{{styles.chatsList}}'>
    {{#each chatItems}}
      {{{this}}}
    {{/each}}
  </ul>
  <div class='{{styles.createCharButton}}'>
    {{{CreateChatButton}}}
    {{{CreateChatModal}}}
  </div>
</section>

<div class={{styles.dividerV}}></div>

{{{Chat}}}
